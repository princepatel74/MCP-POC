---
interface Props {
  href: string;
  size?: "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "inverted" | "muted" | "aura" | "aura-outline" | "ghost";
  class?: string;
  [x: string]: any;
}

const {
  href,
  block,
  size = "lg",
  style = "primary",
  class: className,
  ...rest
} = Astro.props;

const styleMap: Record<string, string> = {
  primary: "btn btn-primary",
  aura: "btn btn-primary",
  outline: "btn btn-outline",
  "aura-outline": "btn btn-outline-cyan",
  inverted: "btn btn-inverted",
  muted: "btn btn-muted",
  ghost: "btn btn-ghost",
};

const sizeMap: Record<string, string> = {
  md: "btn-md",
  lg: "btn-lg",
};
---

<a
  href={href}
  {...rest}
  class:list={[
    styleMap[style] ?? "btn btn-primary",
    sizeMap[size],
    block && "btn-block",
    className,
  ]}
  ><slot />
</a>
