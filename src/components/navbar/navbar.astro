---
import Container from "@/components/container.astro";
import Link from "@/components/ui/link.astro";
import Dropdown from "./dropdown.astro";
import { company } from "@/data/site";
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";

const menuitems = [
  {
    title: "Work",
    children: [
      { title: "Case Studies", path: "/case-studies" },
      { title: "Blog", path: "/blog" },
      { title: "FAQs", path: "/faq" },
    ],
  },
  { title: "Solutions", path: "/solutions" },
  { title: "Services", path: "/services" },
  {
    title: "Company",
    children: [
      { title: "About Us", path: "/about" },
      { title: "Careers", path: "/about#careers" },
    ],
  },
  { title: "Contact", path: "/contact" },
];
---

<Container>
  <header class="sticky top-0 z-50 my-4">
    <div
      class="ai-glass ai-glow-border flex flex-col lg:flex-row justify-between items-center rounded-2xl px-4 py-3 lg:px-6">
      <Astronav>
        <div class="flex w-full lg:w-auto items-center justify-between">
          <a href="/" class="flex items-center gap-2.5 text-lg group">
            <span
              class="flex h-9 w-9 items-center justify-center rounded-lg bg-gradient-to-br from-cyan-500 to-violet-600 text-sm font-bold text-white shadow-md shadow-cyan-500/20 group-hover:shadow-cyan-500/40 transition"
              >S</span
            >
            <span class="font-bold text-brand-900">{company.name}</span>
          </a>
          <div class="block lg:hidden">
            <MenuIcon class="w-5 h-5 text-brand-900" />
          </div>
        </div>
        <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
          <ul class="flex flex-col lg:flex-row lg:gap-1">
            {
              menuitems.map((item, index) => (
                <>
                  {item.children && (
                    <Dropdown
                      title={item.title}
                      children={item.children}
                      lastItem={index === menuitems.length - 1}
                    />
                  )}
                  {!item.children && (
                    <li>
                      <a
                        href={item.path}
                        class="flex lg:px-3 py-2 items-center text-slate-600 hover:text-brand-700 font-medium transition">
                        {item.title}
                      </a>
                    </li>
                  )}
                </>
              ))
            }
          </ul>
          <div class="lg:hidden flex items-center mt-4">
            <Link href="/contact" size="md" style="aura" block>Request a Demo</Link>
          </div>
        </MenuItems>
      </Astronav>
      <div class="hidden lg:flex items-center gap-3">
        <span class="hidden xl:inline-flex items-center gap-1.5 text-[10px] font-medium uppercase tracking-wider text-cyan-700">
          <span class="h-1.5 w-1.5 rounded-full bg-cyan-500 animate-pulse"></span>
          MCP-enabled site
        </span>
        <Link href="/contact" size="md" style="aura">Request a Demo</Link>
      </div>
    </div>
  </header>
</Container>
